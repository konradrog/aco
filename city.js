class City {
    constructor(name, x, y, capacity) {
        this.name = name;
        this.x = x;
        this.y = y;
        this.capacity = capacity;
    }

    get xx() {
        return this.x;
    }

    get yy() {
        return this.y;
    }
}